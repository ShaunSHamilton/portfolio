{
  "name": "portfolio",
  "author": "Shaun Hamilton",
  "version": "1.0.0",
  "private": true,
  "homepage": "http://shaunshamilton.github.io/portfolio/",
  "scripts": {
    "dev:client": "API_LOCATION=http://localhost:3000 npm run -w=client start",
    "dev:server": "npm run -w=server dev",
    "start:server": "npm run -w=server start",
    "build:client": "npm run -w=client build",
    "build:server": "npm run -w=server build",
    "deploy": "npm run -w=client deploy"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": "18.x",
    "npm": "8.x"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
